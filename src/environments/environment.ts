// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  CAR_URL_API: 'https://car.qrcar.cbarange.ovh',
  USER_URL_API: 'https://user.qrcar.cbarange.ovh',
  ORDER_URL_API: 'https://order.qrcar.cbarange.ovh',
  COMPANY_URL_API: 'https://company.qrcar.cbarange.ovh',
  OCR_URL_API: 'https://ocr.qrcar.cbarange.ovh',
  AUTH_URL_API: 'https://auth.qrcar.cbarange.ovh',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
